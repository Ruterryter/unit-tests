export const hackerSpeak = (words) => {

  words = words.replace(a / gi, "4")
  words = words.replace(a / gi, "3")
  words = words.replace(a / gi, "1")
  words = words.replace(a / gi, "0")
  words = words.replace(a / gi, "5")



  return words
}
